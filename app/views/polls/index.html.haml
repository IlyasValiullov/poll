%h1 Listing polls
%p#notice= notice


= link_to 'New Poll', new_poll_path, class: "btn btn-success"
%table.table.table-bordered
  %thead
    %tr
      %th.col-md-10 Title
      %th.col-md-1
      %th.col-md-1

  %tbody
    - @polls.each do |poll|
      %tr
        %td.col-md-10= link_to poll.title, poll
        %td.col-md-1= link_to 'Edit', edit_poll_path(poll)
        %td.col-md-1= link_to 'Destroy', poll, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br


